#!/bin/sh
# askyn
# Usage: askyn

askyn(){
  while :; do
    printf "$* [y/n] "
    read shelib_askyn_resp;
    case $shelib_askyn_resp in
      y) return 0; ;;
      n) return 1; ;;
      *) echo Speifiy y or n.; break; ;;
    esac
  done
}
